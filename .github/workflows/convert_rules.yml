name: Convert Rules

on: [push]

jobs:
  convert_rules:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run Python script
      run: 
        mkdir -p rules && python scripts/convert_rules.py
        
    #- name: Setup Debug Session
    #  uses: csexton/debugger-action@master
        
    - name: Commit and Push
      run: |
        git config --local user.email "1924943697@qq.com"
        git config --local user.name "Qoo-330ml"
        git config --global credential.helper cache
        git add rules/*.txt
        git commit -m "Add generated file to rules folder"
        git push https://github.com/Qoo-330ml/Mycode HEAD:${{ github.ref }}
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}

